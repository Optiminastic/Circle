/**
 * Central brand strings. Change them here once — every header, footer, email
 * subject, and page title reads from this.
 */
export const BRAND = {
  /** Wordmark shown next to the Optiminastic logo. */
  name: 'Circle',
  /** Company name — used in candidate-facing email subjects. */
  company: 'Optiminastic',
  /** Short form for tight spaces / second line. */
  short: 'Circle',
  /** Product name (app title, proctor footer). */
  product: 'Circle',
  /** Sign-off used by HR-facing copy. */
  hrTeam: 'Circle HR Team',
  /** Company site the logo links to. */
  url: 'https://optiminastic.com',
} as const;
